<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WhatsApp QR Code</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #25D366, #128C7E);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .qr-container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            box-shadow: 0 20px 60px rgba(0,0,0,0.1);
        }
        .qr-code {
            max-width: 300px;
            border-radius: 15px;
            margin: 20px 0;
        }
    </style>
</head>
<body>
    <div class="qr-container">
        <h2 class="mb-4">
            <i class="fab fa-whatsapp text-success"></i>
            WhatsApp Bot
        </h2>
        
        <% if (connectionState === 'connected') { %>
            <div class="text-center">
                <i class="fas fa-check-circle fa-5x text-success mb-3"></i>
                <h4 class="text-success">Connected Successfully!</h4>
                <p class="text-muted">Your bot is ready to receive messages.</p>
                <a href="/" class="btn btn-success">Go to Dashboard</a>
            </div>
        <% } else if (qrCode) { %>
            <div>
                <h4 class="mb-3">Scan QR Code to Connect</h4>
                <img src="<%= qrCode %>" alt="WhatsApp QR Code" class="qr-code img-fluid">
                <div class="mt-3">
                    <p><strong>How to Connect:</strong></p>
                    <ol class="text-start">
                        <li>Open WhatsApp on your phone</li>
                        <li>Tap Menu → Linked Devices</li>
                        <li>Tap "Link a Device"</li>
                        <li>Point your phone at this QR code</li>
                    </ol>
                </div>
            </div>
        <% } else { %>
            <div class="text-center">
                <div class="spinner-border text-primary" role="status"></div>
                <h4 class="mt-3">Generating QR Code...</h4>
                <p class="text-muted">Please wait...</p>
            </div>
        <% } %>
        
        <div class="mt-4">
            <a href="/" class="btn btn-outline-primary">Dashboard</a>
            <button class="btn btn-outline-secondary ms-2" onclick="location.reload()">Refresh</button>
        </div>
    </div>

    <script>
        // Auto refresh every 5 seconds if not connected
        <% if (connectionState !== 'connected') { %>
        setTimeout(() => {
            location.reload();
        }, 5000);
        <% } %>
    </script>
</body>
</html>